extends:
  - recommended

apis:
  pocketlab@latest:
    root: contracts/generated/openapi.json

rules:
  no-empty-servers: error
  operation-4xx-response: warn
  operation-operationId: warn
  operation-summary: warn
  no-unused-components: warn
  security-defined: off
  info-license: off
