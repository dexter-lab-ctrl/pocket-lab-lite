---
title: "Vocabulary"
description: "Canonical status semantics and independent documentation dimensions."
generated: true
audience: knowledgebase
confidence: generated
source_commit: uncommitted
generated_at: uncommitted
generator: scripts/docs/knowledge/generate_knowledge.py
generator_version: 2
---

# Vocabulary

| Status | Exact meaning | Does not prove | Dimensions | Blocks promotion | Blocks writes | Can coexist |
| --- | --- | --- | --- | --- | --- | --- |
| `accepted-limitation` | A known constraint is explicitly accepted for a bounded scope. | Healthy runtime behavior. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `capture-failed` | Repository-defined status `capture-failed`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `contract-derived` | The fact is derived from a generated or canonical contract such as OpenAPI, AsyncAPI, or parity metadata. | The contract was exercised successfully at runtime. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `degraded` | Operational runtime health is reduced or a safe read/write guard is active. | Semantic mismatch. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `drift-detected` | Repository-defined status `drift-detected`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `healthy` | Available runtime health signals satisfy the current health policy. | Release readiness or complete semantic parity. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `implemented` | Repository-defined status `implemented`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | implementation_status | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `inferred` | Repository-defined status `inferred`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `joining` | Repository-defined status `joining`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `mapped` | Repository-defined status `mapped`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `match` | Repository-defined status `match`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `mismatch` | Repository-defined status `mismatch`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `missing` | Repository-defined status `missing`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `needs-review` | Repository-defined status `needs-review`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `not-applicable` | The field or condition has no applicable runtime object for the observed scenario. | Missing implementation. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `not-observed` | The required or optional runtime fact was not captured in the evidence lane. | The underlying feature is absent. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `observed` | A runtime lane successfully captured the field or state. | Semantic agreement with another lane or operational health. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `offline` | Repository-defined status `offline`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `online` | Repository-defined status `online`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `partial` | The capability or domain is not fully implemented or not fully verified. | Failure of the portions that are implemented. | implementation_status | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `patch-provided` | Repository-defined status `patch-provided`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `planned` | The model intentionally reserves a future capability that is not implemented as current truth. | Any current runtime capability. | implementation_status | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `protected server host` | Repository-defined status `protected server host`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `ready` | Repository-defined status `ready`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `ready-with-accepted-limitations` | Readiness may proceed while explicitly tracked limitations remain accepted. | Full runtime verification without constraints. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `release-promoted` | Evidence was explicitly promoted and bound to a release/source identity. | All domains are verified or operationally healthy. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `repairing` | Repository-defined status `repairing`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `runtime-observed` | A runtime observation contributed to the knowledge fact. | Promotion or cross-lane verification. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `runtime-unavailable` | Repository-defined status `runtime-unavailable`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `source-derived` | The fact is deterministically derived from repository-owned source or canonical metadata. | Live runtime state. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `stale` | Repository-defined status `stale`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `stale-evidence` | Repository-defined status `stale-evidence`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `unsupported` | Repository-defined status `unsupported`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `unvalidated` | Implementation or metadata exists without the evidence required for validation. | Broken behavior. | implementation_status | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `verified` | Appropriate contract or runtime evidence confirms the expected semantic condition. | Every underlying subsystem is currently operationally healthy. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `verified-with-mapped-presentation` | Backend/runtime meaning is verified while the frontend uses an explicitly allowlisted user-facing representation. | Literal string equality between backend and UI. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
| `waiting` | Repository-defined status `waiting`; consult its owning contract before applying it across dimensions. | A status in one documentation dimension does not automatically prove another dimension. | — | policy-dependent | policy-dependent | Independent status dimensions may coexist unless an owning contract explicitly forbids the combination. |
