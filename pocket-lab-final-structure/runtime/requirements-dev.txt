# Pocket Lab development and test dependencies
# Includes runtime deps plus tooling for backend contract tests,
# NATS integration tests, WebSocket tests, formatting, linting,
# pre-commit, and architecture validation.

-r requirements.txt

# Backend tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
anyio>=4.0.0

# Quality gates
ruff>=0.6.0
black>=24.0.0
pre-commit>=3.7.0
detect-secrets>=1.5.0

# YAML / IaC / Ansible validation
yamllint>=1.35.0
ansible-core>=2.16.0
ansible-lint>=24.0.0

# Optional but useful for runtime/dev parity
python-dotenv>=1.0.0
