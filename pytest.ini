[pytest]
testpaths =
    tests
python_files =
    test_*.py
pythonpath =
    tests
    runtime
addopts =
    --strict-markers
    --strict-config
norecursedirs =
    .git
    .venv
    node_modules
    dist
    site
    storybook-static
    pocket-lab-final-structure
    pocket-lab-main
